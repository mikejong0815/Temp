// import { fromJS } from 'immutable';
// import { makeSelectPasswordRestorePageDomain } from '../selectors';

// const selector = makeSelectPasswordRestorePageDomain();

describe('makeSelectPasswordRestorePageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
