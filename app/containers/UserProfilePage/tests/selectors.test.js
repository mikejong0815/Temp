// import { fromJS } from 'immutable';
// import { makeSelectUserProfilePageDomain } from '../selectors';

// const selector = makeSelectUserProfilePageDomain();

describe('makeSelectUserProfilePageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
