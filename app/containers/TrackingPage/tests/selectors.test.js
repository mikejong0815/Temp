// import { fromJS } from 'immutable';
// import { makeSelectTrackingPageDomain } from '../selectors';

// const selector = makeSelectTrackingPageDomain();

describe('makeSelectTrackingPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
